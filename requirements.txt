streamlit
requests
sentence-transformers
numpy
# Optional local-only extras (not needed for cloud; app degrades gracefully):
#   chromadb   - local ChromaDB vector store (needs Ollama nomic embeddings)
#   ollama     - local LLM answers + nomic embeddings
